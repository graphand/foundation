export enum SubscriptionChannels {
  EMAIL = "email", // Specfic email address
  ACCOUNT = "account", // Specific account (email of the account)
  ROLE = "role", // All accounts with a specific role
}
