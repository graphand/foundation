export enum ValidatorTypes {
  REQUIRED = "required",
  UNIQUE = "unique",
  BOUNDARIES = "boundaries",
  LENGTH = "length",
  REGEX = "regex",
  SAMPLE = "sample",
  KEY_PROPERTY = "keyProperty",
  DATAMODEL = "datamodel",
  EXISTS = "exists",
}
