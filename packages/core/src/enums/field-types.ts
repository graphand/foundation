export enum FieldTypes {
  ID = "id",
  ARRAY = "array",
  TEXT = "text",
  NUMBER = "number",
  BOOLEAN = "boolean",
  RELATION = "relation",
  DATE = "date",
  NESTED = "nested",
  IDENTITY = "identity",
}
