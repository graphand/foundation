export enum PropertyTypes {
  DEFAULT = "default",
  ID = "id",
  ARRAY = "array",
  STRING = "string",
  NUMBER = "number",
  INTEGER = "integer",
  BOOLEAN = "boolean",
  RELATION = "relation",
  DATE = "date",
  OBJECT = "object",
  IDENTITY = "identity",
  NULL = "null",
}
