const env = {
  APP_HOSTNAME: String(process.env.APP_HOSTNAME),
  GRAPHAND_CLIENT_ENDPOINT: String(process.env.GRAPHAND_CLIENT_ENDPOINT),
  GRAPHAND_CLIENT_SSL: String(process.env.GRAPHAND_CLIENT_SSL),
};

export default env;
